import ko = require('knockout');
require(["jQuery"], function ($) {
  $(document).ready(function () {
    //instantie le view-model de la page
    ko.applyBindings(/* l'instance en question*/);
  });
});